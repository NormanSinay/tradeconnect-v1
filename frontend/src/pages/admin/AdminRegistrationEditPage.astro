---
import { AdminLayout } from '@/layouts/AdminLayout'
import AdminRegistrationEditPage from '@/components/admin/AdminRegistrationEditPage'

export async function getStaticPaths() {
  // TODO: Generate paths based on registrations
  return []
}

const { registrationId } = Astro.params

const breadcrumbs = [
  { label: 'Dashboard', href: '/admin' },
  { label: 'Inscripciones', href: '/admin/inscripciones' },
  { label: `Inscripción #${registrationId}`, href: `/admin/inscripciones/${registrationId}/detalle` },
  { label: 'Editar' },
]

const title = `Editar Inscripción #${registrationId}`
---

<AdminLayout {title} {breadcrumbs}>
  <AdminRegistrationEditPage registrationId={parseInt(registrationId)} client:only="react" />
</AdminLayout>