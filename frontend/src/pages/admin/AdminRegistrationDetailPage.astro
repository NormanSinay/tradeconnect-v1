---
import { AdminLayout } from '@/layouts/AdminLayout'
import AdminRegistrationDetailPage from '@/components/admin/AdminRegistrationDetailPage'

export async function getStaticPaths() {
  // TODO: Generate paths based on registrations
  return []
}

const { registrationId } = Astro.params

const breadcrumbs = [
  { label: 'Dashboard', href: '/admin' },
  { label: 'Inscripciones', href: '/admin/inscripciones' },
  { label: `Inscripción #${registrationId}`, href: '#' },
]

const title = `Detalle de Inscripción #${registrationId}`
---

<AdminLayout {title} {breadcrumbs}>
  <AdminRegistrationDetailPage registrationId={parseInt(registrationId)} client:load />
</AdminLayout>