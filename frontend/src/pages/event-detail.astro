---
import BaseLayout from '@/layouts/BaseLayout.astro'
import EventDetailPage from '@/components/ui/event-detail-page'

const url = new URL(Astro.request.url)
const eventId = url.searchParams.get('id')
---

<BaseLayout title="Detalle del Evento - TradeConnect">
  <EventDetailPage eventId={eventId} client:only="react" />
</BaseLayout>