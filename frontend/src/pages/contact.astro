/**
 * @fileoverview contact.astro - Página de contacto de TradeConnect
 * @description Página de contacto con formulario de envío de mensajes, información de contacto y enlaces a redes sociales.
 *
 * Arquitectura recomendada:
 * React (componentes interactivos)
 *   ↓
 * Astro (routing y SSR)
 *   ↓
 * shadcn/ui (componentes UI)
 *   ↓
 * Tailwind CSS (estilos)
 *   ↓
 * Radix UI (primitivos accesibles)
 *   ↓
 * Lucide Icons (iconos)
 *
 * @author TradeConnect Team
 * @version 1.0.0
 */
---
import BaseLayout from '@/layouts/BaseLayout.astro';
---

<BaseLayout
  title="Contacto - TradeConnect"
  description="Ponte en contacto con TradeConnect. Estamos aquí para ayudarte con cualquier pregunta sobre eventos, inscripciones o nuestra plataforma."
>
  <div class="contact-page">
    <section class="hero-section">
      <div class="container">
        <div class="breadcrumb">
          <a href="/" class="breadcrumb-link">← Volver al Inicio</a>
        </div>
        <h1>Contáctanos</h1>
        <p class="hero-description">
          Estamos aquí para ayudarte. Envíanos un mensaje y te responderemos lo antes posible.
        </p>
      </div>
    </section>

    <section class="contact-section">
      <div class="container">
        <div class="contact-grid">
          <div class="contact-form-container">
            <h2>Envíanos un mensaje</h2>
            <form class="contact-form" id="contactForm">
              <div class="form-group">
                <label for="name">Nombre completo *</label>
                <input type="text" id="name" name="name" required placeholder="Tu nombre completo" />
              </div>

              <div class="form-group">
                <label for="email">Correo electrónico *</label>
                <input type="email" id="email" name="email" required placeholder="tu@email.com" />
              </div>

              <div class="form-group">
                <label for="phone">Teléfono</label>
                <input type="tel" id="phone" name="phone" placeholder="+502 1234-5678" />
              </div>

              <div class="form-group">
                <label for="subject">Asunto *</label>
                <select id="subject" name="subject" required>
                  <option value="">Selecciona un asunto</option>
                  <option value="general">Consulta general</option>
                  <option value="events">Información sobre eventos</option>
                  <option value="registration">Problemas con registro</option>
                  <option value="payment">Consultas de pago</option>
                  <option value="fel">Facturación FEL</option>
                  <option value="organizer">Quiero ser organizador</option>
                  <option value="other">Otro</option>
                </select>
              </div>

              <div class="form-group">
                <label for="message">Mensaje *</label>
                <textarea id="message" name="message" required rows="6" placeholder="Escribe tu mensaje aquí..."></textarea>
              </div>

              <button type="submit" class="btn-primary">Enviar mensaje</button>
            </form>
          </div>

          <div class="contact-info-container">
            <h2>Información de contacto</h2>

            <div class="contact-info-card">
              <div class="info-icon">📧</div>
              <h3>Email</h3>
              <p>info@tradeconnect.gt</p>
              <p class="info-secondary">soporte@tradeconnect.gt</p>
            </div>

            <div class="contact-info-card">
              <div class="info-icon">📞</div>
              <h3>Teléfono</h3>
              <p>+502 1234-5678</p>
              <p class="info-secondary">Lunes a Viernes: 8:00 AM - 6:00 PM</p>
            </div>

            <div class="contact-info-card">
              <div class="info-icon">📍</div>
              <h3>Oficina principal</h3>
              <p>Ciudad de Guatemala</p>
              <p class="info-secondary">Guatemala, C.A.</p>
            </div>

            <div class="contact-info-card">
              <div class="info-icon">⏰</div>
              <h3>Horario de atención</h3>
              <p>Lunes - Viernes: 8:00 AM - 6:00 PM</p>
              <p class="info-secondary">Sábados: 9:00 AM - 1:00 PM</p>
            </div>

            <div class="social-links">
              <h3>Síguenos en redes sociales</h3>
              <div class="social-icons">
                <a href="https://facebook.com/tradeconnect" target="_blank" rel="noopener noreferrer" class="social-icon">📘</a>
                <a href="https://twitter.com/tradeconnect" target="_blank" rel="noopener noreferrer" class="social-icon">🐦</a>
                <a href="https://instagram.com/tradeconnect" target="_blank" rel="noopener noreferrer" class="social-icon">📷</a>
                <a href="https://linkedin.com/company/tradeconnect" target="_blank" rel="noopener noreferrer" class="social-icon">💼</a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="faq-section">
      <div class="container">
        <h2>¿Necesitas ayuda rápida?</h2>
        <p class="section-description">Consulta nuestras preguntas frecuentes o visita nuestro centro de ayuda</p>
        <div class="cta-buttons">
          <a href="/faq" class="btn-secondary">Preguntas frecuentes</a>
          <a href="/help" class="btn-outline">Centro de ayuda</a>
        </div>
      </div>
    </section>
  </div>
</BaseLayout>

<style>
  .contact-page {
    min-height: 100vh;
  }

  .hero-section {
    background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
    color: white;
    padding: var(--spacing-2xl) 0;
    text-align: center;
  }

  .breadcrumb {
    text-align: left;
    margin-bottom: var(--spacing-lg);
  }

  .breadcrumb-link {
    color: white;
    text-decoration: none;
    font-size: 0.95rem;
    opacity: 0.9;
    transition: opacity 0.3s ease;
    display: inline-flex;
    align-items: center;
    gap: var(--spacing-xs);
  }

  .breadcrumb-link:hover {
    opacity: 1;
    text-decoration: underline;
  }

  .hero-section h1 {
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: var(--spacing-md);
    font-family: var(--font-secondary);
  }

  .hero-description {
    font-size: 1.25rem;
    max-width: 700px;
    margin: 0 auto;
    opacity: 0.9;
  }

  .contact-section {
    padding: var(--spacing-2xl) 0;
    background: var(--background);
  }

  .contact-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-2xl);
  }

  .contact-form-container h2,
  .contact-info-container h2 {
    color: var(--text-primary);
    font-family: var(--font-secondary);
    margin-bottom: var(--spacing-xl);
    font-size: 1.75rem;
  }

  .contact-form {
    background: white;
    padding: var(--spacing-xl);
    border-radius: var(--border-radius-lg);
    box-shadow: var(--shadow-md);
  }

  .form-group {
    margin-bottom: var(--spacing-lg);
  }

  .form-group label {
    display: block;
    color: var(--text-primary);
    font-weight: 600;
    margin-bottom: var(--spacing-sm);
    font-size: 0.9rem;
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    width: 100%;
    padding: var(--spacing-md);
    border: 1px solid var(--border-color);
    border-radius: var(--border-radius-md);
    font-size: 1rem;
    font-family: var(--font-primary);
    transition: all 0.3s ease;
  }

  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--primary);
    box-shadow: 0 0 0 3px rgba(107, 30, 34, 0.1);
  }

  .form-group textarea {
    resize: vertical;
  }

  .btn-primary {
    width: 100%;
    padding: var(--spacing-md);
    background: var(--primary);
    color: white;
    border: none;
    border-radius: var(--border-radius-md);
    cursor: pointer;
    font-weight: 600;
    font-size: 1rem;
    transition: background 0.3s ease;
  }

  .btn-primary:hover {
    background: var(--primary-dark);
  }

  .contact-info-container {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-lg);
  }

  .contact-info-card {
    background: white;
    padding: var(--spacing-lg);
    border-radius: var(--border-radius-lg);
    box-shadow: var(--shadow-sm);
    border-left: 4px solid var(--primary);
  }

  .info-icon {
    font-size: 2rem;
    margin-bottom: var(--spacing-sm);
  }

  .contact-info-card h3 {
    color: var(--primary);
    font-size: 1.1rem;
    margin-bottom: var(--spacing-sm);
    font-family: var(--font-secondary);
  }

  .contact-info-card p {
    color: var(--text-primary);
    margin-bottom: var(--spacing-xs);
    font-weight: 500;
  }

  .info-secondary {
    color: var(--text-secondary);
    font-size: 0.9rem;
    font-weight: 400;
  }

  .social-links {
    background: white;
    padding: var(--spacing-lg);
    border-radius: var(--border-radius-lg);
    box-shadow: var(--shadow-sm);
  }

  .social-links h3 {
    color: var(--text-primary);
    font-size: 1.1rem;
    margin-bottom: var(--spacing-md);
    font-family: var(--font-secondary);
  }

  .social-icons {
    display: flex;
    gap: var(--spacing-md);
  }

  .social-icon {
    width: 50px;
    height: 50px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--surface);
    border-radius: 50%;
    font-size: 1.5rem;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .social-icon:hover {
    background: var(--primary);
    transform: translateY(-3px);
  }

  .faq-section {
    background: linear-gradient(135deg, var(--surface) 0%, white 100%);
    padding: var(--spacing-2xl) 0;
    text-align: center;
  }

  .faq-section h2 {
    color: var(--text-primary);
    font-size: 2rem;
    margin-bottom: var(--spacing-md);
    font-family: var(--font-secondary);
  }

  .section-description {
    color: var(--text-secondary);
    font-size: 1.1rem;
    margin-bottom: var(--spacing-xl);
  }

  .cta-buttons {
    display: flex;
    gap: var(--spacing-md);
    justify-content: center;
  }

  .btn-secondary {
    padding: var(--spacing-md) var(--spacing-xl);
    background: var(--secondary);
    color: white;
    border: none;
    border-radius: var(--border-radius-md);
    cursor: pointer;
    font-weight: 600;
    font-size: 1rem;
    transition: background 0.3s ease;
    text-decoration: none;
    display: inline-block;
  }

  .btn-secondary:hover {
    background: var(--secondary-dark);
  }

  .btn-outline {
    padding: var(--spacing-md) var(--spacing-xl);
    background: transparent;
    color: var(--primary);
    border: 2px solid var(--primary);
    border-radius: var(--border-radius-md);
    cursor: pointer;
    font-weight: 600;
    font-size: 1rem;
    transition: all 0.3s ease;
    text-decoration: none;
    display: inline-block;
  }

  .btn-outline:hover {
    background: var(--primary);
    color: white;
  }

  @media (max-width: 968px) {
    .hero-section h1 {
      font-size: 2rem;
    }

    .hero-description {
      font-size: 1rem;
    }

    .contact-grid {
      grid-template-columns: 1fr;
    }

    .cta-buttons {
      flex-direction: column;
      align-items: center;
    }

    .btn-secondary,
    .btn-outline {
      width: 100%;
      max-width: 300px;
    }
  }
</style>

<script>
  document.getElementById('contactForm')?.addEventListener('submit', function(e) {
    e.preventDefault();
    alert('Gracias por tu mensaje. Te contactaremos pronto.');
    this.reset();
  });
</script>
