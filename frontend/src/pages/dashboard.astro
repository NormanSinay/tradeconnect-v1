/**
 * @fileoverview dashboard.astro - Página de redirección al dashboard de administración
 * @description Página que redirige automáticamente a la aplicación React para el dashboard administrativo.
 *
 * Arquitectura recomendada:
 * React (componentes interactivos)
 *   ↓
 * Astro (routing y SSR)
 *   ↓
 * shadcn/ui (componentes UI)
 *   ↓
 * Tailwind CSS (estilos)
 *   ↓
 * Radix UI (primitivos accesibles)
 *   ↓
 * Lucide Icons (iconos)
 *
 * @author TradeConnect Team
 * @version 1.0.0
 */
---
// Dashboard page for admin users
// This is a client-side rendered React app entry point
---

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - TradeConnect</title>
</head>
<body>
    <div id="app"></div>
    <script>
        // Redirect to React app which handles dashboard routing
        window.location.href = '/';
    </script>
</body>
</html>
